"""app.common.context — intentionally minimal.

Correlation-ID propagation has been removed. This module is kept so that
any import that was already pruned from the codebase does not produce an
ImportError during the transition period.
"""
